require "spec_helper"

describe Notification::Hub::Client do
  it "has a version number" do
    expect(Notification::Hub::Client::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
